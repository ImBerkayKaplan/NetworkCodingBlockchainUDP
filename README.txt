This project mainly uses network coding.
The project utilizes UDP to send small packets. 
The network coding library arranges the packet to compose the message
The name of the network coding library is NCUtils
The goal of this program is to send a message to all nodes on the localhost.
The program then sends the received message to all other nodes to verify the client sent the same message to all.
